secret = 'Fff123'
pw = ''
auth=False
count =0
max_temp=5

while pw != secret:
    count +=1
    if count > max_temp: break
    pw = input(f"{count}: What's the secret word? ")
else:
    auth = True
print("Thank you" if auth else "Please check your password.")

